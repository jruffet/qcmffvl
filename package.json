{
  "name": "qcmffvl",
  "version": "4.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node scripts/sync-data.js && vite --host --no-open",
    "build": "node scripts/sync-data.js && vite build",
    "lint": "eslint *.js \"web/js/**/*.{js,ts}\" --no-warn-ignored",
    "test": "vitest"
  },
  "devDependencies": {
    "@types/node": "^25.6.0",
    "eslint": "^9.0.0",
    "eslint-plugin-angular": "^1.0.0",
    "eslint-plugin-import": "^2.29.1",
    "nodemon": "^3.1.14",
    "npm-check-updates": "^20.0.1",
    "typescript": "^6.0.2",
    "vite": "^8.0.8",
    "vitest": "^4.1.4"
  }
}
