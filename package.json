{
  "name": "qcm-ffvl",
  "version": "4.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "(vite --host --no-open & npx nodemon --watch web/json/versions.json --exec \"node scripts/sync-data.js\" --quiet)",
    "build": "node scripts/sync-data.js && vite build",
    "lint": "eslint web/js/**/*.js --no-warn-ignored"
  },
  "devDependencies": {
    "vite": "^8.0.8",
    "nodemon": "^3.1.14",
    "eslint": "^9.0.0",
    "eslint-plugin-angular": "^1.0.0",
    "eslint-plugin-import": "^2.29.1",
    "npm-check-updates": "^20.0.1"
  }
}
